{
  "buildCommand": "turbo run build --filter=./apps/museum-portal",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "pnpm install",
  "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }]
}
